import { Title } from '@angular/platform-browser';

export interface Admin {
    id?: number,
    Fullname?: string,
    Fechana?: Date,
    Correo?: string,
    Contrasena?: string,
    Imagenadmin?: string
};