import { Title } from '@angular/platform-browser';

export interface Citaa {
    id?: number,
    Fechacita?: Date,
    Pago?: boolean,
    AdminId?: number,
    PacienteId?: number,
    HorarioId?: number,
    Estado?:string
  }
